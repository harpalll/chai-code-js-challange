{
  "name": "node-starter",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build day5/function.js --out-dir dist"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "axios": "^1.7.2"
  }
}
